CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);


CREATE TABLE X (
  PRIMARY KEY (y),
  FOREIGN KEY (y) REFERENCES X(ID)
);





















